#!name=🔐 加密DNS模块 (DoH3优先)
#!desc=使用DoH3/DoH加密DNS，保护隐私防止DNS污染。IPv4+IPv6混合，国内外DNS并行查询。
#!author=nowaytouse
#!homepage=https://github.com/nowaytouse/script_hub
#!icon=https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Lock.png

[General]
# ═══════════════════════════════════════════════════════════════
# DNS服务器配置 (IPv4 + IPv6 混合)
# ═══════════════════════════════════════════════════════════════
# 优先级: system > 国内DNS > 国际DNS
# - system: 使用系统DNS，确保本地服务正常解析
# - 223.5.5.5: 阿里DNS (国内快速)
# - 119.29.29.29: 腾讯DNSPod (国内快速)
# - 1.1.1.1: Cloudflare (国际快速)
# - 8.8.8.8: Google DNS (国际稳定)
# - 9.9.9.9: Quad9 (安全过滤)
dns-server = system, 223.5.5.5, 119.29.29.29, 1.1.1.1, 8.8.8.8, 9.9.9.9

# ═══════════════════════════════════════════════════════════════
# 加密DNS服务器 (DoH3/DoH)
# ═══════════════════════════════════════════════════════════════
# DoH3 (DNS over HTTP/3) 优先 - 更快更安全
# h3:// = HTTP/3 (QUIC协议，更低延迟)
# https:// = HTTP/2 (传统DoH，兼容性好)
#
# 国际DNS:
# - Cloudflare: 全球最快，隐私保护
# - Google: 稳定可靠
# - Quad9: 安全过滤恶意域名
# - AdGuard: 广告过滤
# - ControlD: 隐私保护
#
# 国内DNS:
# - 阿里DNS: 国内快速
# - DNSPod: 腾讯DNS
encrypted-dns-server = h3://cloudflare-dns.com/dns-query, h3://dns.google/dns-query, h3://dns.quad9.net/dns-query, h3://dns.adguard-dns.com/dns-query, h3://freedns.controld.com/p2, https://dns.alidns.com/dns-query, https://doh.pub/dns-query

# 加密DNS跟随出站模式 (代理时使用代理DNS)
encrypted-dns-follow-outbound-mode = true

# 不跳过证书验证 (安全)
encrypted-dns-skip-cert-verification = false

# ═══════════════════════════════════════════════════════════════
# DNS劫持 - 强制所有DNS请求经过Surge
# ═══════════════════════════════════════════════════════════════
hijack-dns = *:53, *:5353

# ═══════════════════════════════════════════════════════════════
# DNS高级选项
# ═══════════════════════════════════════════════════════════════
# 允许SVCB/HTTPS记录 (现代DNS特性)
allow-dns-svcb = true

# 读取本地hosts文件
read-etc-hosts = true
