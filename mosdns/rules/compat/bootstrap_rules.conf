# Flattened rules for bootstrap -> group bootstrap
domain-rules /alidns.com/ -nameserver bootstrap
domain-rules /captive.apple.com/ -nameserver bootstrap
domain-rules /cloudflare-dns.com/ -nameserver bootstrap
domain-rules /connectivitycheck.gstatic.com/ -nameserver bootstrap
domain-rules /cp.cloudflare.com/ -nameserver bootstrap
domain-rules /detectportal.firefox.com/ -nameserver bootstrap
domain-rules /dns.adguard-dns.com/ -nameserver bootstrap
domain-rules /dns.alidns.com/ -nameserver bootstrap
domain-rules /dns.google/ -nameserver bootstrap
domain-rules /dns.google.com/ -nameserver bootstrap
domain-rules /dns.nextdns.io/ -nameserver bootstrap
domain-rules /dns.pub/ -nameserver bootstrap
domain-rules /dns.quad9.net/ -nameserver bootstrap
domain-rules /dns4all.eu/ -nameserver bootstrap
domain-rules /doh.360.cn/ -nameserver bootstrap
domain-rules /doh.dns.apple.com/ -nameserver bootstrap
domain-rules /doh.mullvad.net/ -nameserver bootstrap
domain-rules /doh.opendns.com/ -nameserver bootstrap
domain-rules /doh.pub/ -nameserver bootstrap
domain-rules /dot.pub/ -nameserver bootstrap
domain-rules /libredns.gr/ -nameserver bootstrap
domain-rules /mirror.baidu.com/ -nameserver bootstrap
domain-rules /nssurge.com/ -nameserver bootstrap
domain-rules /sgponte.com/ -nameserver bootstrap
domain-rules /wikimedia-dns.org/ -nameserver bootstrap
domain-rules /www.gstatic.com/ -nameserver bootstrap
