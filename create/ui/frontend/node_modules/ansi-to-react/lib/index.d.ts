import * as React from "react";
declare interface Props {
    children?: string;
    linkify?: boolean | "fuzzy";
    className?: string;
    useClasses?: boolean;
}
export default function Ansi(props: Props): React.ReactElement;
export {};
