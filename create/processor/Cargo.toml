[package]
name = "rust_processor"
version = "0.1.0"
edition = "2024"
[lib]
name = "rust_processor"
crate-type = ["staticlib"]

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
regex = "1.10"
once_cell = "1.19"
walkdir = "2.5.0"
